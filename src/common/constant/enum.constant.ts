export enum Role {
  USER = 'user',
  ADMIN = 'admin',
  MANAGER = 'manager',
}

export enum UserStatus {
  PUBLIC = 'public',
  PRIVACY = 'privacy',
}

export enum Status {
  BLOCK = 'block',
  REJECTED = 'rejected',
  FOLLOW = 'follow',
  PENDING = 'pending',
  FRIEND = 'friend',
}
